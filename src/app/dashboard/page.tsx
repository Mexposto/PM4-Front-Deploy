import Dashboard from "@/components/Dashboard/Dashboard";

const page = () => {
  return (
    <div className="flex flex-col items-center w-full min-h-screen bg-primary text-secondary">
      <Dashboard />
    </div>
  );
};

export default page;
