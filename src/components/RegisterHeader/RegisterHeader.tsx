const RegisterHeader = () => {
  return (
    <header className="w-full max-w-md h-16 bg-accent text-white text-lg font-semibold rounded-lg flex items-center justify-center shadow-md">
      Register
    </header>
  );
};

export default RegisterHeader;
